var timeInMinutes;
timeInMinutes = prompt(timeInMinutes, []);
var jumps;
jumps = (timeInMinutes / 10) * 3;

var message;
message = 'За ' + timeInMinutes + ' минут в соцсетях полагается ' + jumps + ' прыжков.';
console.log(message);